# The official site's compiled bundle, downloaded only as a reverse-engineering
# reference. It is xsyd.top's copyrighted code and is deliberately NOT redistributed.
index.js

# Local static-server noise
server.log

# Same for the v2 app's chunks, pulled from https://www.xsyd.top/v2-app/ to check
# whether the vendor implements calibration / travel test (it does not).
v2-*.js

# Scratch Playwright QA pass (screenshots + a local playwright install) - not app content.
.qa-scratch/
